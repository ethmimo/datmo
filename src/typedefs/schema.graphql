type Profile {
  username: String
  bio: String
}
type Query {
  sources: [String]
  index(id: String): [String]
  profile(username: String): Profile
}

type Mutation {
  indexProfile(id: String): [String]
}

schema {
  query: Query
  mutation: Mutation
}
